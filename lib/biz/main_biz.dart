import 'package:flutter/material.dart';

class MainBiz extends StatelessWidget {
  const MainBiz({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Container(child: Center(child: Text("Biz"),),),);
  }
}
